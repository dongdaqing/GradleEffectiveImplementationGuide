def printTaskName = { task ->
  println "Run ${task.name}"
}

task second << printTaskName
second.dependsOn {
  project.tasks.findAll { task ->
    task.name.contains 'f'
  }
}

task fist << printTaskName
task beforeSecond << printTaskName
